<?xml version="1.0" encoding="utf-8"?>
<org.schabi.newpipe.views.FocusAwareDrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <org.schabi.newpipe.views.FocusAwareCoordinator
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/fragment_holder"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginTop="?attr/actionBarSize" />

            <include
                layout="@layout/toolbar_layout"
                android:id="@+id/toolbar_layout"/>

            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/fragment_player_holder"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center_horizontal"
                app:behavior_hideable="true"
                app:behavior_peekHeight="0dp"
                app:layout_behavior="org.schabi.newpipe.player.event.CustomBottomSheetBehavior" />

        </org.schabi.newpipe.views.FocusAwareCoordinator>

        <!-- Bottom nav sits below the coordinator (not inside it), so the
             mini-player bottom sheet above is naturally bounded by the
             coordinator's own (shorter) height and never overlaps this bar -
             no special peekHeight math needed, avoids a real risk of the
             two overlapping since neither could be visually tested here. -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            app:itemIconTint="@color/contrastColor"
            app:itemTextColor="@color/contrastColor"
            app:menu="@menu/menu_bottom_navigation" />

    </LinearLayout>

    <include
        layout="@layout/drawer_layout"
        android:id="@+id/drawer_layout" />

</org.schabi.newpipe.views.FocusAwareDrawerLayout>
